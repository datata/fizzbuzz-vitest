import { describe, expect, it } from "vitest";

const fizzbuzz = () => {
    
};

describe('fizzbuzz', () => {
    it('should be a function', () => {
        expect(typeof fizzbuzz).toBe('function')
    })
});